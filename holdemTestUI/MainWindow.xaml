<Window x:Class="holdemTestUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:holdemTestUI.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000"
        d:DataContext="{d:DesignInstance local:ViewModel}">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="4*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel >
            <ListBox ItemsSource="{Binding Player}" Height="100" Width="100" Margin="5"/>
            <ListBox ItemsSource="{Binding Player[1]}" Height="100" Width="100" Margin="5"/>
            
            

        </WrapPanel>

        <WrapPanel Grid.Row="2" >
            <Button Command="{Binding StartTournamentCommand}" Width=" 100 " Height="100">init</Button>
            <Button Command="{Binding ShuffleCommand}" Width=" 100 " Height="100">Shuffle</Button>
            <Button Command="{Binding DrawCardCommand}" Width=" 100 " Height="100">Draw</Button>
        </WrapPanel>

    </Grid>
</Window>
